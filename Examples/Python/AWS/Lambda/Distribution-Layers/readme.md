# Distribution Layers

## Instructures to make a layer

Using AWS Cloud9 Development environment

~~~
mkdir {target}_layer/python
cd {target}_layer
pip install {target} -t python
zip -r {target}_layer.zip python
~~~