<VirtualHost *:80>
    DocumentRoot /var/www/html

    # Additional configurations (like ErrorLog, CustomLog) can go here.

    # Add a Directory section for the DocumentRoot
    <Directory /var/www/html>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>